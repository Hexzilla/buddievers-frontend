import { Theme } from '@mui/material';

export const styles = (theme: Theme) => ({
  footerWrapper: {
    background: "#000",
    color: "#FFFFFF",
    padding: "4vw"
  },
  title:{
    fontWeight: "900",
    fontSize: "30px"
  },
  text: {
    fontWeight: "400",
    fontSize: "20px"
  }
});
