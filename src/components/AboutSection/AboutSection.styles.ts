import { Theme } from '@mui/material';

export const styles = (theme: Theme) => ({
    leftContainer: {
        background: "#FFF",
        textAlign: "center",
        padding: "3vh",
    },
    aboutText: {
        fontSize: "96px",
        fontWeight: "900",
        color: "#00CE4C"
    },
    subTitleText: {
        fontWeight: "900",
        fontSize: "40px"
    }
});
