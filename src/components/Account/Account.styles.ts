import { Theme } from '@mui/material';

export const styles = (theme: Theme) => ({
  button: {
    padding: '6px 16px',
    background: '#F5E2B2',
    borderRadius: '10px',
    color: '#01472A',
    fontWeight: '900'
    /*
    [theme.breakpoints.down('sm')]: {
      background: 'none',
      position: 'absolute',
      right: 0,
    },
    */
  },
});
