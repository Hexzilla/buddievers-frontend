import { Theme } from '@mui/material';

export const styles = (theme: Theme) => ({
  button: {
    padding: '6px 16px',
    background: '#111',
    borderRadius: '0',
    /*
    [theme.breakpoints.down('sm')]: {
      background: 'none',
      position: 'absolute',
      right: 0,
    },
    */
  },
});
