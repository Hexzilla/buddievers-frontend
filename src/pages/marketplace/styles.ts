import { Theme } from '@mui/material';

export const styles = (theme: Theme) => ({
    container : {
        paddingLeft : '5vw',
        paddingRight : '5vw',
        background : "#016B43",
        paddingBottom : "50px"
    }
});